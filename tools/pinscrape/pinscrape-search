#!/usr/bin/env bash
# Wrapper so you can run `pinscrape-search "<query or search url>"` from anywhere.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/.venv/bin/python" "$DIR/search_scrape.py" "$@"
